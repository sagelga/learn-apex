@isTest
public with sharing class TestVerifyDate {
    @isTest static void test1(){
        // VerifyDate.CheckDates(date1, date2);
        // VerifyDate.CheckDates(date1, date2);
        // VerifyDate.CheckDates(date1, date2);
    }
}
